mutation {
    createTestPlan(
        testIssueIds: []
        jira: {
            fields: {
                summary: "%SUMMARY_NAME%",
                project: {key: "BP"}
            }
        }
    ) {
        testPlan {
            issueId
            jira(fields: ["key"])
        }
        warnings
    }
}